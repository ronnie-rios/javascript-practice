const nums = [ 10, 100, 200, 3000, 5000] //return the amount of numbers that have odd num of digits

function countTheZero(nums) {
  let count;

  for(let i=0; i<nums.length;i++) {
    let num = nums[i].split('');
    for(let j =0; j< num[j]; j++) {
      num[j] 
    }
  }
}